import styled from '@emotion/styled';
import { Container } from '@mui/material';

export const StyledApp = styled(Container)`
  min-height: 100vh;
  width: 100%;
  display: flex;
  gap: 100px;
  padding: 50px;
`
